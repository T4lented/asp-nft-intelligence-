services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nft_intel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d nft_intel"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: .
    command: sh -c "node dist/db/migrate.js && node dist/server.js"
    env_file: .env
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build: .
    command: sh -c "node dist/db/migrate.js && node dist/worker.js"
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
